# Anti-slop audit 001 — AyushSetu portal (ayush-portal/) — 2026-09-20

Mode: AFTER. Scope: light ayurveda re-theme, Jobs Insights tab, logbook proof, em-dash sweep.
Dials: ENERGY 2 / RHYTHM 2 / MOTION 1.
Design read: internship + upskilling portal for BAMS students, warm paper + herb-green professional style.

## Findings (pick numbers to fix; unmentioned numbers are not touched)

1. [HIGH] Ananya hero story lost its demo label (R-18, R-38). It now reads as a real student outcome.
   Fix: eyebrow becomes "Sample journey: Ananya, BAMS final year".
2. [MEDIUM] Mono uppercase wide-tracked eyebrows still head most sections (R-06).
   Fix: keep mono for measurement (scores, counts), switch prose eyebrows to semibold sans.
3. [MEDIUM] Reveal fade-up on every section with identical params (R-19).
   Fix: keep reveals on hero + first proof section only, render the rest static.
4. [MEDIUM] Jobs tabs are mouse-only (R-26, R-32). role=tab without arrow keys or tabpanels.
   Fix: add arrow-key roving focus and tabpanel wiring, or drop tab roles for plain buttons.
5. [LOW] index.html title still says "Ayush Portal, Vaidya Track" while the brand is AyushSetu (R-20).
   Fix: retitle "AyushSetu: BAMS internships + upskilling".
6. [LOW] Curated seed stipends/deadlines read as live figures (R-17).
   Fix: one line under the feed tabs: "Sample listings to start your pipeline. Confirm dates on the source site."
7. [LOW] Feed cards are identical rows (R-14). Earned: they are data rows and the decision
   (eligible/apply stage) drives the layout, so no action. Listed for the record.

## Already clean (no action)
- R-02: user-visible em dashes swept (coach, ATS reasons, briefings, courses, interview bank).
- R-09: capsule pills carry real counts (tab counts) or track labels, never hype words.
- R-24/R-26: every nav item routes; every button acts (save/apply/pipeline/proof).
- R-27: feed empty state, live-board error state, resume empty state all present.
- R-29: palette is paper + white + herb green + one warm neutral. Accent lands on primary actions only.
