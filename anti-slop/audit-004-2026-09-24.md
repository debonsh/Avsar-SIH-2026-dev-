# Anti-slop audit 004 — GOV pass: Welcome calm-down, TechHome console, AyushHome board — 2026-09-24

Mode: AFTER. Scope: `Welcome.jsx`, `TechHome.jsx`, `AyushHome.jsx` rewrites.
Previous audits: 001 (1-7), 002 (8-21), 003 (22-28). Numbering continues from 29.
Direction pivot (user): clean GOV-like corporate. Dials now ENERGY 1 /
RHYTHM 2 / MOTION 1 (was ENERGY 2 with Duolingo-loud choice). Consequences:
entrance stagger and press scale removed site-wide; hierarchy carries the
design; the three screens hold three different structures (door, ledger plus
docket, board tiles plus rounds) so the sameness ends.

R-31 one-line reasons for the pass:
- Welcome flat: the door is a formality, one headline, no decoration budget.
- TechHome ledger: four numbers with hairline rules read as a console ledger.
- TechHome docket rows: learn, apply, ask is a sequence, rows show order.
- AyushHome board keeps tiles plus round cards: a notice board pins notices.
- Numerals shared across homes: one measurement voice, two structures.

## Findings (pick numbers to fix; unmentioned numbers are not touched)

29. [HIGH] TechHome ledger uses `dl` without `dt`/`dd` (a11y, semantic HTML).
    Evidence: `TechHome.jsx` ledger `dl` wraps `LedgerCell` divs of plain `p`.
    Screen readers expect definition structure; this is a stat block, not one.
    Fix: `dl` becomes a labelled `div`; cells unchanged.
30. [LOW] Welcome `rounded-2xl` and Ayush hero `rounded-3xl` escape the square
    system (R-11). Evidence: theme zeroes `--radius-lg/xl` only, so 2xl/3xl
    render visibly round against square panels. Fix: both become `rounded-xl`.
31. [LOW] `src/lib/motion.js` has no component consumers left (C-1).
    Evidence: Welcome dropped stagger/press; `ui.jsx` never imported it. Only
    `tests/motion.test.js` touches it. No action now: it is the token spine
    for later steps (lists, toasts). Listed so a future pass does not
    re-hardcode numbers.

## Already clean (no action)

- R-01: orbs, dither, gradient, and leaf watermark removed; emerald-900 and
  zinc-950 flats with documented reasons.
- R-02: all touched copy swept (headings, nudges, gap lines); missing-value
  placeholder is the en dash, matching the shell ring precedent.
- R-05/R-14: three distinct compositions (door choice, ledger plus docket,
  tiles plus rounds); equal-weight choices keep equal cards with a written
  reason (finding 23 carries over).
- R-06: mono kept for status strips, numerals, readings; prose stays sans.
- R-19/MOTION 1: zero entrance animation on all three screens; hover, focus,
  and native press states only. `Reveal` removed from both homes.
- R-25: Ayush tiles and round cards moved to `tone="light"` (dark-on-dark
  fixed in both themes via the remap layers); ledger zinc-50 on zinc-950.
- Vercel: single h1 per screen, hierarchical h2s, decorative icons
  `aria-hidden`, native buttons and links, `min-w-0` on flex content,
  `tabular-nums` on figures, `text-balance` on headlines, hover states on
  every action, no forms/media/lists-over-50 touched.

## Fix report (2026-09-24, 29/30 approved and fixed)

- 29: ledger `dl` became a labelled `div` (`role="group"`); cells unchanged.
- 30: Welcome hero `rounded-2xl` and Ayush header `rounded-3xl` both became
  `rounded-xl` (square through the theme tokens).

Verify: 303/304 tests (only the pre-existing market failure);
`npm run build` clean; `npm run lint` 3 pre-existing warnings.

## Pre-existing failure (not mine, untouched)

- `tests/market.test.js` postingVolume still fails exactly as in audit-003
  (verified again this pass: 303/304). Logic-step owner needed.
