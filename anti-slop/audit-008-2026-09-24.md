# Anti-slop audit 008 — Market Pulse light-theme pass — 2026-09-24

User: Market Pulse carries dark/light bugs, dark elements sitting in light
theme. Full re-read of the 659-line page plus charts.jsx against all four
remap layers. The page is zinc-first and mostly safe; the breakage was all in
the bright status greens/ambers, which no light layer remaps.

## Findings (all fixed this pass)

49. [HIGH] `text-amber-400` numerals/meta invisible on white cards in both
    light themes (~1.6:1). Fix: `text-amber-300`, the one amber both lights
    remap dark (#b45309) while both darks keep light (#fcd34d). Touched: the
    three Stat numerals, weight column, unheld postings counts.
50. [HIGH] `text-emerald-400` data text washed out on white in tech-light
    (only ayush-light remaps it). Fix: `text-green-300`, which both lights map
    dark (#15803d) and both darks keep light (#86efac). Touched: weight > 1,
    employer counts, pay bands, all six explainer bullets. Nine instances,
    one replaceAll, grep confirms zero remain.
51. [MED] HeatGrid zero-cells painted `INK.grid` (#27272a): correct empty on
    dark, reads as filled on white. Fix (`charts.jsx`): empty cells render
    transparent so the panel shows through on either ground.

## Audited clean (no action)

- LaneSwitch, Stat cards, wire feed, table tracks/borders, employer tiles +
  skill chips, donut center, RankBars/Columns/Stacked/WeightArc/Sparkline SVG
  ink (saturated data colors + remapped zinc labels), Badges/Chips/Empty,
  saturated dot/fill accents (`emerald-500`, `sky-400`, `amber-500`,
  `zinc-600` dots all visible on white).

## Follow-up (hover wash, same day)

52. [MED] Demand-table row hover `hover:bg-zinc-900/60` smeared dark grey over
    white rows in light themes (opacity variant, unmapped). Fix: exact
    `hover:bg-zinc-900` (light grey wash in lights, subtle lift in darks).
    Swept all `hover:bg-*/opacity` remains: the three left are white washes on
    always-dark saturated surfaces, correct everywhere.

## Follow-up 2 (grey hover removed, same day)

53. [LOW] The replacement grey wash still read as decoration on
    non-interactive rows, so the hover is gone entirely: demand-table rows
    (MarketPulse) and syllabus rows (ui.jsx SyllabusTable) have no hover
    state. Hover feedback now exists only where a row is actionable.

## Follow-up 3 (heat-grid edges, same day)

54. [MED] HeatGrid cells drew fixed `border-zinc-900` edges: dark outlines on
    every box in light themes. Fix (`charts.jsx`): empty cells now use exact
    `border-zinc-800 bg-zinc-900` (light fill + light edge in lights, subtle
    dark grid in darks); filled cells drop the border and separate by the 3px
    table gaps, so no dark rim survives on either ground.

## Fix report (2026-09-24, 49-54 applied)

Verify: 412/412 tests; `npm run build` clean; `npm run lint` pre-existing
warnings only.
