# Anti-slop audit 005 — full sweep: Journey + all remaining pages — 2026-09-24

Mode: AFTER, autonomous (user: finish all steps, critique, fix, test, report).
Scope: Journey, Resume, Quests, Jobs, Interview, Profile, Match, MarketPulse,
Portfolio, Workspace, Quiz, Verify, Industry, Institute, Faculty, Programs,
Public, Ayush landing, NotFound, StreakMeter. Previous audits: 001 (1-7),
002 (8-21), 003 (22-28), 004 (29-30). Numbering continues from 32.
MarketPulse and NotFound read clean, no changes. Public/Programs touched only
for copy. Ayush landing keeps its Reveal + map (landing owns the motion
budget; the scrim overlay is readability, not decoration).

## Findings (all fixed this pass)

32. [HIGH] Interview questionnaire conditional inverted (logic bug, not taste).
    Evidence: `Interview.jsx` rendered `{!qnrLoading ? <Empty "Reading your
    resume"> : bank form}`. Keyless, resume-less, and AI-resolved states all
    showed the skeleton forever; the form only appeared mid-load by accident.
    Fix: `{qnrLoading ? Empty : form}`. Bank-first promise now holds.
33. [MED] ayush-dark contrast traps on dark colored panels. Evidence: Industry
    openFit `bg-emerald-950` + `text-emerald-300`, Portfolio revoked
    `bg-red-950` + `text-red-300`, Verify revoked card same pair. ayush-dark
    flips those bgs light while the text stays light. Fix: border-l callouts
    with zinc ink (no fill, no theme risk); Verify revoked uses a plain Card
    with `border-red-900`.
34. [MED] `text-zinc-700` on dark (Jobs filter row, Jobs recent-activity event)
    unmapped in both dark themes. Fix: `text-zinc-300` (remapped in lights,
    light in darks, all four hold).
35. [MED] Profile cover gradients (emerald band, zinc-to-blurple band). Fix:
    flat `bg-emerald-900` / `bg-zinc-900`. The card is an ID badge, not a poster.
36. [LOW] Institute roster rows `border-zinc-100` render near-white in dark
    themes (unmapped). Fix: `border-zinc-800` (remapped light in lights).
37. [LOW] Square discipline: Jobs mission + closing `rounded-2xl`, Jobs company
    tile `rounded-2xl`, Profile avatar `rounded-2xl`, Ayush landing panels
    `rounded-2xl` x2. Fix: all `rounded-xl` (square through theme tokens).
38. [LOW] Tap targets under 44px: Journey tabs, Jobs section tabs, Profile
    skill/year pills + clear button, Programs tabs + provider links, Faculty
    official-page link, Industry feed link, Workspace apply link, Quiz option
    rows (`min-h-[44px]`), QuestCheck invisible hit-area (`before:-inset-2.5`,
    visual unchanged).
39. [LOW] Copy sweep: em dashes removed from every user-facing JSX string
    (final grep over `src/**/*.jsx` string literals: zero); lowercase sentence
    starts fixed (Jobs explainer, Industry labels/errors/empty/actions);
    Quiz timer emoji dropped for words; quiz radio accent lane-aware;
    Profile stat icons lane-aware; Workspace sign-off flash contradiction
    ("verified under review") replaced with a plain confirmation;
    Resume file input unified to the blurple-button style.

## Deliberate keeps (no action)

- Verify terminal-lowercase voice; `&rsquo;` typography; `→`/`↗` only as nav
  affordances; `Lx → Ly` only as compact gap notation; Profile readiness "—"
  (shell precedent); Quests check spring and Profile step slide
  (user-initiated, reduced-motion respected); Quests motion import retained.

## Fix report (2026-09-24, all 32-39 applied)

Verify: 332/333 tests (only the pre-existing market postingVolume failure);
`npm run build` clean; `npm run lint` pre-existing warnings only
(taxonomy, wire DAY, coach-widget deps, charts/ui fast-refresh).
