# Anti-slop audit 009 — favicon, Resume layout, onboarding, smoothness — 2026-09-24

User: fix the favicon, smoothen UI, improve onboarding, rebuild the bad
Resume scoring layout. Previous audits: 001-008 (1-54). Numbering from 55.

## Findings (all fixed this pass)

55. [MED] Favicon was a leftover purple blob matching neither portal. Fix:
    new `public/favicon.svg` (blurple tile, white A); head metadata neutral
    for dual-portal (title, description); pre-paint body + manifest colors set
    to ink so first paint matches the dark-first theme instead of flashing paper.
56. [MED] Resume result layout rebuilt (same engine, same copy sources).
    Before: cramped sticky rail beside an overloaded column, videos as a
    maybe-empty stack. After: full-width score strip (number, rank, formula,
    stale warning), fixes beside dimensions, skills beside sections, one video
    card per gap skill in a 3-up row, plus a "How to read this report"
    signpost. Also fixed while inside: tech "Write this" ink went zinc-100
    (both themes hold).
57. [LOW] Onboarding late steps felt like an interrogation: answers vanished
    behind you. Fix: `PicksRecap` mono strip ("so far: role · lane · skills")
    atop the goal and availability steps. Back links on all four steps raised
    to 44px touch targets. (A concurrent rewrite already replaced
    `transition-all` with scoped `transition-[scale,...]` + ease-out, which is
    the smoother primitive this audit would have asked for.)
58. [LOW] Smoothness: `scroll-behavior: smooth` gated behind
    `prefers-reduced-motion: no-preference`, so anchor/chat jumps glide while
    the reduced-motion path stays instant.

## Fix report (2026-09-24, 55-58 applied)

Verify: 413/413 tests; `npm run build` clean; `npm run lint` 5 pre-existing
warnings.
