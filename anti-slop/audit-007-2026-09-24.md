# Anti-slop audit 007 — translucent fills in light themes — 2026-09-24

Follow-up to 006 (user: dark elements persist on Proof of Skill + Career GPS).
Root cause found: `bg-zinc-900/40` tiles. The remap layers use exact class
selectors (`.tech-light .bg-zinc-900`), which never match the `/40` opacity
variant, so those tiles stay dark-grey on white cards in both light themes
while everything around them flips. Readable, but visibly wrong.

## Findings (all fixed this pass)

47. [MED] `bg-zinc-900/40` tiles unmapped in light themes. Fix: solid
    `bg-zinc-900` (exact class, remapped everywhere). Touched: Challenges
    RubricBox + grade preview, CareerGps postings box + course tiles,
    Shortlist rows, Labs rows, MarketPulse employer tiles. The one remaining
    `/60` instance is a transient row hover, kept deliberately.
48. [LOW] CareerGps checkboxes used `accent-blurple` on both portals. Fix:
    lane-aware (`accent-[#1e7a4c]` on ayush), matching the Quiz precedent.

## Checked clean

- CareerGps sim banner (`bg-blurple/5`, faint wash, remapped ink): readable
  everywhere, left alone. Stop numerals, connectors, picked/unpicked roles:
  exact classes, all remapped.

## Fix report (2026-09-24, 47-48 applied)

Verify: 412/412 tests; `npm run build` clean; `npm run lint` pre-existing
warnings only.
