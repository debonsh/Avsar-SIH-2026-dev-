# Anti-slop audit 003 — Step 1: Welcome rebuild + motion foundation — 2026-09-24

Mode: AFTER (user chose AFTER). Scope: `src/pages/Welcome.jsx` (rewrite),
`src/lib/motion.js` + `tests/motion.test.js` (new), `onboarding.js:86` read.
Previous audits: 001 (1-7), 002 (8-21). Numbering continues from 22.
Skills active: antislop core + antislop-ui + motion-foundations + motion-patterns
+ web-design-guidelines (repo-local install, guidelines fetched fresh for this audit).

Design read: front door for student interns (BAMS + tech), system24 chrome
(mono readings, 1px square panels, dither with purpose) with a Duolingo-loud
choice moment, dial ENERGY 2 / RHYTHM 2 / MOTION 1.

R-31 one-line reasons for the rebuild:
- Headline 6xl Bricolage: the door gets one shout, everything else whispers.
- Orbs stay: emerald/blurple are the two universes, not decoration.
- Cards stagger once on mount: orientation for first-timers, the only motion.
- Press scale on cards: the tap IS the product (one tap sets the portal).
- Mono numerals on the loop: lesson stages, measured, not decorated.

## Findings (pick numbers to fix; unmentioned numbers are not touched)

22. [HIGH] Router question renders an em dash as the Step-1 h2 (R-02).
    Evidence: `onboarding.js:86` `q: "AYUSH or Tech — where do you belong?"`
    renders at `Welcome.jsx` h2. `Welcome.jsx` itself is em-dash free.
    Fix: `AYUSH or Tech: where do you belong?`
23. [MEDIUM] Three option cards share one layout (R-14). Earned: equal choices
    need equal weight; universes split by badge color + icon, active state by
    border. No action. Listed for the record.
24. [MEDIUM] `meta theme-color #f6f3ea` vs the dark door (Vercel dark-mode rule).
    Evidence: `index.html:8` ships the paper tint while Welcome owns a dark
    floor; Android chrome tints wrong on first paint.
    Fix: `#06060b` default (per-portal tint is shell-step work).
25. [LOW] No `color-scheme` declaration (Vercel dark-mode rule).
    Evidence: `index.css` never sets it; form controls/scrollbars fall back.
    Fix: `:root { color-scheme: dark }` plus light overrides on
    `.ayush-light` / `.tech-light`.
26. [LOW] Interview-path question copy carries em dashes (R-02, Journey step).
    Evidence: `onboarding.js:27,51` upskill questions. Out of Step-1 scope.
    Fix with the Journey rebuild, not here.
27. [LOW] `touch-action: manipulation` missing (Vercel touch rule).
    Evidence: tap-highlight is set, double-tap delay guard is not.
    Fix: one line on `body` in `index.css`.
28. [LOW] Sentence-case voice vs Vercel Title Case rule. Deliberate repo voice
    (matches every screen); changing it would break identity (R-20, R-31).
    No action. Listed for the record.

## Already clean (no action)

- R-01: orbs documented as universe colors; no new gradients, glow, glass.
- R-06: mono kept for readings + numerals; prose eyebrows stay sans.
- R-09/R-15/R-16: no badges, no generic CTAs ("Continue to dashboard",
  "Edit profile"), no buzzwords on the page.
- R-17/R-18/R-36: no statistics, no testimonials, no claims.
- R-19/MOTION 1: one entrance stagger (0.08s, inside the 0.05-0.10 band) +
  press feedback; hero and trio static; `shouldAnimate() && !reduce` gate.
- R-24/R-26: every control acts (`go()` to /home, dashboardFor, /profile,
  compare buttons); motion.button keeps native keyboard behavior.
- R-25/R-32: footer microcopy lifted to zinc-500; native buttons inherit the
  global `:focus-visible` ring plus the Button-token ring.
- Foundations rules: `motion/react` only; transform/opacity only; tokens and
  springs imported from `lib/motion.js`, zero inline numbers; no `layout` on
  subtrees; no `transition: all` (motion `transition` prop is the spring map,
  CSS side uses `transition-colors`/`transition-transform` explicitly).
- Vercel a11y: single h1, hierarchical h2, decorative icons `aria-hidden`,
  no icon-only buttons, `tabular-nums` on numerals, `text-balance` on
  headlines. No forms, media, or lists over 50 on this page.

## Fix report (2026-09-24, 22/24/25/27 approved and fixed)

- 22: `onboarding.js:86` em dash becomes a colon. No test asserts the string.
- 24: `index.html` theme-color `#f6f3ea` becomes `#06060b`.
- 25: `:root { color-scheme: dark }` plus light overrides on
  `.ayush-light` / `.tech-light` in `index.css`.
- 27: `touch-action: manipulation` added to `body` in `index.css`.

Verify: 303/304 tests pass (only the pre-existing market failure above);
`npm run build` clean; `npm run lint` 3 pre-existing warnings.

## Pre-existing failure (not mine, untouched)

- `tests/market.test.js` "postingVolume buckets real dates by week" fails
  identically with and without my files (verified via targeted `git stash` of
  my four files: 303 pass / 1 fail both ways). Logic-step owner needed; my
  five `tests/motion.test.js` tests pass.
