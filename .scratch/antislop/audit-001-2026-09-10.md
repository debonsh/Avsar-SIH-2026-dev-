# antislop audit 001 — 2026-09-10 (After mode, scope: app/src UI + copy + comments)

Design Read: task tool for students (Operate), incumbent zinc-dark voice, dial ENERGY 2 / RHYTHM 2 / MOTION 1.
User pre-authorized all fixes ("make changes"). Audit path uses .scratch/ per repo convention, not anti-slop/.

## HIGH (Hard Gate)

1. R-02 — ~15 em dashes in user-facing strings (statusLine, empty states, Step heads, fit chip, battle/quizzes/interview notes, error strings, Gemini prompt). Fix: periods/colons.
2. R-02 — em dashes in ~15 code comments (`// ponytail`, amicro headers). Fix: commas.
3. R-32 — sort `<select>` (App.jsx) has `outline-none` with no focus replacement. Fix: add focus style.
4. R-03 — mic button 40px, quest toggles 20px (< 44px min). Fix: 44px mic, 24px toggles (dense-list note).
5. R-26/R-35 — click-through every interactive by inspection (no browser CDP here); user smoke-tests in Brave.

## MEDIUM (Purpose-Gate, fix or write reason)

6. R-19 — TextReveal on all 5 in-app H1s replays on every view switch: wallpaper. Fix: plain headings; keep reveal on landing hero only (purpose: first-impression focal moment).
7. R-05/R-14 — landing "How it works" is exactly 3 identical cards. Fix: 4-step funnel list matching the app's real STEPS (Role, Upload, Score, Fix), numbered list instead of cards.
8. R-08 — arrows on 3 text CTAs. Fix: keep only "Save score → battle" (purpose: forward motion in the scoring funnel); drop the other two.
9. R-04 — Lucide-only set; Sparkles on AI actions. Keep: single consistent set suits an Operate tool; sparkles marks genuinely-AI actions only.
10. R-09 — "AI on / Local mode" badge. Keep: real status label, no glow/dot/uppercase combo.
11. R-06 — Inter + eyebrow caps. Keep: Inter for tabular micro-numbers legibility; caps eyebrows separate label from value.

## LOW (Quality Locks + honesty)

12. Code — 8 `{/* ──/══ */}` banner comments. Fix: plain labels.
13. R-16 — "Easiest to unlock" sort label. Fix: "Lowest score first". ("Unlock the badge" stays: literal game verb.)
14. R-17/R-36/R-38 — numbers audit. PASS: landing strip computed live; battle seeds labeled "demo". Privacy line is literal (the file never uploads; text transmits only on explicit AI click).
15. R-21/R-29/R-11/R-10 — dark fixed (focus tool, incumbent identity); zinc/white + emerald, amber/red semantic only; radius lg/md/full by role; blur on sticky header only.
16. Dead code — `ROADMAP` export unused since quests view replaced it. Fix: delete.

## Resolution (same day)

Fixed 1, 2 (all UI/error/prompt strings + all code comments; third-party course titles and the sample-resume fixture exempt with reason recorded), 3 (select focus style), 4 (44px mic, 24px quest toggles), 6 (plain in-app headings, reveal kept on landing hero only), 7 (landing is now a 4-step numbered funnel list matching the app's real STEPS), 8 (only "Save score → battle" keeps its arrow), 12 (plain section labels), 13 ("Lowest score first"), 16 (ROADMAP deleted). Kept with written reasons: 9, 10, 11, 14, 15. Verified: tests 4/4, full SSR render (home funnel present, zero em dashes in render), `npm run build` green.

